query GetVsphereVMs(
    $first: Int!
)
{
    snappableConnection(
        filter: {
            objectType: VmwareVirtualMachine
        }
        first: $first
    )
    {
        nodes
        {
            name
            fid
        }
    }
}
